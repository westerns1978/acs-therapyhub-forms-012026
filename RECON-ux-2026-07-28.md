# RECON — UI/UX professional-grade audit — 2026-07-28

READ-ONLY recon. main @ c2ec809 · live bundle index-BhiB5qhp.js.
Scale frame: ~34 clients today (21 active, 1 session); production target 80–100 active across 5 counselors.
Constraints honored: Attesta parity (prefer token/component-level), 42 CFR Part 2 exposure, prior UX passes not re-litigated.

---

## TOP 10 — ranked backlog

| # | Finding | Sec | Effort | Impact | Holds at 100 clients? |
|---|---|---|---|---|---|
| 1 | **Fail-open error class** — `/compliance-readiness` renders a green "every enforceable rule is met" on query failure (`complianceEngine.ts:547`); Dashboard ×4 (`Dashboard.tsx:83-112`), staff form queue (`ClientSubmissionsPanel.tsx:36-44`), portal dashboard ("Session Expired." misdiagnosis, `PortalDashboard.tsx:178-218`) | 3 | **S** | A compliance product that reassures on failure. Patterns to copy already exist in-repo (Financials, PortalErrorCard) | Severity **grows** with caseload |
| 2 | **Global search returns N dead links** — every Command Palette client row navigates to trial-hidden `/program-compliance/:id` → silent bounce to /dashboard (`CommandPalette.tsx:39-44`); ditto "Messages" (`:38`); Clara's `navigate_to_page` has no allowlist (`ClaraContext.tsx:239,359,541`) | 1b | **XS–S** | The Ctrl+K search is 100% broken for clients right now | Worse — more clients = more dead rows |
| 3 | **122 dead-token classes render no color** — `text-on-surface-secondary` ×78 etc. across 22 files (DEFERRED #24 recorded 431× on 7/15; today's grep counts 122 — method differs, fix identical): define the tokens once in `index.html` colors block | 4b | **XS** define + **S** verify | Washed-out labels app-wide fixed in one file; ports to Attesta as one hunk | Yes |
| 4 | **Display-label bundle** — `programLabel()` exists but 12 surfaces render raw `OPIOID_RECOVERY` etc. (worst: Guardrails card `Dashboard.tsx:225`); RiskMonitor renders **"Cip/Oep/Wip"** (`RiskMonitor.tsx:29-36` — a bug); portal has a 5-key shadow map (`usePortalClient.ts:14-20`); "1 SESSIONS TODAY" (`Dashboard.tsx:321-322`); 24-hour times on Today's Schedule (`:167-168`); 6 duplicate currency formatters, 0 `Intl.NumberFormat` | 2 | **S** | ~20 one-line swaps + 3 tiny shared helpers; kills the most-seen raw tokens | Yes |
| 5 | **Semantic status tokens** — no success/warning/danger/info token exists; 4,812 raw palette uses vs 1,060 token uses. Extend the proven `slate`-override trick (`index.html:38-44`) to amber/emerald/red/rose/blue + add semantic tokens → ~1,500 sites become a one-file edit | 4b | **S–M** | THE Attesta-parity lever; converts future re-themes from sweep to edit | Yes |
| 6 | **Vocabulary: one word per concept** — the alert feed has 5 names (sidebar "Compliance Risk" / h1 "Risk Monitor" / card "Open alerts" / Clara "compliance flag"); guardrail verdicts have 5; determination/placement both in one sentence twice; nav label ≠ breadcrumb everywhere (no label map, `Breadcrumbs.tsx:60-62`) | 1a | **M** | Every screen reads more professional; mostly string edits + 2 route renames | Yes |
| 7 | **Global print stylesheet** — `/index.css` is a 404 (`index.html:128`); 6 `print:` utilities in 2 files; dark mode prints near-black pages; 30+ routes print garbage incl. SuperbillPreview + CourtReportPreview. ~15 lines once the stylesheet exists | 4f | **S** | Fixes every route at once; court/state-document product | Yes |
| 8 | **Modal.tsx hardening** — shared modal has no focus trap, no Escape (global handler covers a hardcoded list only, `MainLayout.tsx:116-125`), no `aria-labelledby`, unlabeled close X, no backdrop-close (`Modal.tsx:13-41`) | 4d | **S** | One component; every modal in the app inherits | Yes |
| 9 | **Aging signals** — intake queue shows a grey date, no age (6/30 intake looks like this morning's, `Dashboard.tsx:268`; `createdAt` already fetched); unsigned notes show no age anywhere (GreenRoom already *returns* `createdAt` and drops it, `greenRoom.ts:92`); elapsed deadline windows drop their magnitude (`ComplianceDeadlineStrip.tsx:76-85`) | 2c | **S** | Turns three passive lists into work queues; data already loaded | **Required** at 100 — queues are unusable without it |
| 10 | **Long names + dashboard dedup** — `text-4xl` client `<h1>` no truncate (`ClientProfileHeader.tsx:150`), schedule row collides with chevron (`Dashboard.tsx:172`); `alertSummary.total` rendered 4× on one screen, two stat cards are dead numbers; the ONE number (violation count) is never rendered | 3/1c | **XS** truncate, **M** hierarchy | Truncation is 3 class edits; hierarchy is the dashboard telling David where to look | Truncation: yes. Stat cards: yes |

Near-misses (detailed below, pick freely): ClientSelectionGrid N+1 (~140 requests at 60 clients — **M**, the one true scale blocker), demo-data choke-point filter (**S** code + flagged data backfill), Command Palette keyboard nav (**S**), Button/Input primitive (**M–L**, biggest craft lever but not cheap), calendar empty state (**S**), PublicIntake validation (**S**), `font-black` de-escalation (**M**).

---

## SECTION 1 — INFORMATION ARCHITECTURE

### 1a. Vocabulary audit

**The alert/flag/violation/warning/risk cluster: SIX nouns over TWO (arguably five) genuinely distinct data sources, with FIVE independent severity ladders and no shared token module.**

The distinct underlying concepts:

| Data source | Tokens | Defined at |
|---|---|---|
| A. Operational alert feed (`ClientAlert`) | CRITICAL / HIGH / ELEVATED / MODERATE | `services/alertsService.ts:19` |
| B. Rule-engine verdicts (`GuardrailVerdict`) | warning / violation (of met/warning/violation/not_enforceable) | `services/complianceEngine.ts:30,274` |
| C. Cert-gate checklist (packet readiness) | Met / Action needed / Can't verify yet | `components/clients/ClientOverviewTab.tsx:61-79` over `services/packetReadiness.ts:167` |
| D. Deadline clock | On track / Due soon / Verify / Re-screen / Outstanding / Automatic / N/A | `services/complianceClock.ts:42-49` → `components/clients/ComplianceDeadlineStrip.tsx:17-24` |
| E. AI relapse-risk prediction (genuinely distinct "risk") | probability | `pages/ClientWorkspace.tsx:107,117,137` |

**Source A is called five different things, never the same thing twice:** sidebar "Compliance Risk" (`NavigationSidebar.tsx:29`) → page h1 "Risk Monitor" (`RiskMonitor.tsx:65`) → breadcrumb "Risk Monitor" (`Breadcrumbs.tsx:60-62` title-cases the slug) → Dashboard stat card "Open alerts" (`Dashboard.tsx:330`) → Clara's line "…have a compliance flag" (`Dashboard.tsx:147` — the ALERT feed, called a flag).

**Source B is called five things, two on the same screen:** "Clinical Guardrails" (`Dashboard.tsx:200`), "Guardrail flags" stat card (`:338`), "No compliance flags." empty state (`:241`), "Active Compliance Flags" (`ComplianceReadiness.tsx:63`), "Violations"/"Warnings" tiles (`:56-59`). The raw lowercase token `{g.status}` ("violation"/"warning") prints verbatim at `Dashboard.tsx:225` and `ComplianceReadiness.tsx:79`, uppercased by CSS only.

**Head-on collision:** the sidebar item for the *compliance alert feed* is named "Compliance Risk" while "risk" is also the word for the AI relapse prediction ("AI Relapse Risk Prediction" `ClientWorkspace.tsx:107`, "Dimension Risk Analysis" `AsamAssessment.tsx:164`).

**Recommended one-word-per-concept:**

| Concept | Word | Key renames |
|---|---|---|
| Rule-engine verdicts | **Guardrail** | stop calling the same array "flags"/"Compliance Flags" |
| Operational alert feed | **Alert** | sidebar "Compliance Risk"→"Alerts"; h1 "Risk Monitor"→"Alerts"; route `/risk-monitor`→`/alerts` so breadcrumb agrees |
| Cert-gate checklist | **Gate** | already says "N of M gates met" (`ClientOverviewTab.tsx:262`) |
| Deadline obligations | **Deadline** | already consistent |
| AI relapse prediction | **Risk** | reserve "risk" exclusively for this |
| Severity ladders | ONE ladder | currently 4/2/3/7-token ladders + RiskMonitor's TIER_LABEL — five color/label maps, no shared module |

**Other clusters:**

- **session / appointment / meeting** — `appointments` table rendered as "Calendar" (nav, `NavigationSidebar.tsx:68`) / "Session Management" (breadcrumb) / "Sessions" (dead BottomNavBar `:21`) / "Today's Schedule" with role-forked subtitle using BOTH nouns for the same array ("Appointments on the books" vs "Today's clinical sessions", `Dashboard.tsx:155`). ClientWorkspace tab is labeled **"Services"** (`ClientWorkspace.tsx:368`) but its content card says "Session History" (`ClientSessionsTab.tsx:290-309`). Also two different `service_type` concepts already collide — self-documented at `config/sessionTaxonomy.ts:16-18`. Recommend: **Appointment** = the booking row, **Session** = the clinical encounter, rename tab Services→Sessions, pick **Calendar** and rename route `/session-management`→`/calendar`. "Meeting" (`MeetingReportForm.tsx:29`) is genuinely distinct (client's outside AA/NA meetings) — keep.
- **client / patient** — near-clean; ONE violation: `components/SuccessScreen.tsx:38` "your **patient** file". XS fix.
- **note / documentation** — four names for one concept: "Create Note" (`GlobalHeader.tsx:100`), "Smart Note Studio" (`NoteStudioDock.tsx:89`), "Clinical Notes" (`ClientOverviewTab.tsx:345`), "Clinical Documentation" (`ActiveSession.tsx:208`). Recommend **Note**.
- **program / treatment plan** — route `/program-plan/:clientId` renders `TreatmentPlan.tsx` (`App.tsx:33,170`), so the breadcrumb says "Program Plan" while nav/tab/h1 all say "Treatment Plan". Recommend **Treatment Plan** user-facing; reserve **Program** for `programVocab` values; rename route.
- **determination / placement** — one concept, two nouns, sometimes in the same sentence: `ComplianceDeadlineStrip.tsx:50` ("No signed **determination** yet — …after a signed **placement** is completed"), `GreenRoom.tsx:218`. Recommend: **Determination** = the signed artifact/act; **placement** only as resulting state ("placed at Level IV").

### 1b. Nav structure

**Desktop sidebar** (`components/ui/NavigationSidebar.tsx`): ungrouped top list (`:66-77`) = Dashboard, Calendar (`/session-management`), Clients, Forms, Help & Training (Messages + AI Documents filtered by trialMode `:75`). Section **"Reports"** (`:96`, items `:28-35`):

| Label | Route | Belongs under "Reports"? |
|---|---|---|
| Compliance Risk | `/risk-monitor` | ❌ actionable work queue with "Take action" buttons (`RiskMonitor.tsx:138`) |
| Treatment Plan Library | `/treatment-plan-library` | ❌ template picker — a creation tool (`TreatmentPlanLibrary.tsx:142-143`) |
| Compliance Readiness | `/compliance-readiness` | ✅ |
| Financials | `/financials` | ✅ |
| ~~Analytics~~ | `/reporting` | filtered; label ≠ slug ≠ breadcrumb |
| **Settings** | `/settings` | ❌ **confirmed** — administration filed under Reports; also duplicated in profile dropdown (`GlobalHeader.tsx:152-154`) |

The comment at `NavigationSidebar.tsx:25-27` admits the grouping was a density decision, not semantic.

**Mobile drawer is a DIFFERENT tree** (`components/ui/MobileDrawer.tsx`): Settings lives under "Administration" (`:140`) not "Reports"; Treatment Plan Library + Financials are in the main list; **there is no Compliance Risk/`/risk-monitor` entry at all — a Therapist on mobile cannot reach the alert queue from nav**; drawer carries `/compliance` (`:64`, filtered) which desktop doesn't.

**Breadcrumbs have no label map** (`Breadcrumbs.tsx:60-62` title-cases path segments), so every label≠slug pair contradicts the sidebar: Calendar→"Session Management", Compliance Risk→"Risk Monitor", Analytics→"Reporting", plus "Program Plan".

**Dead nav code:** `components/ui/BottomNavBar.tsx` (self-documented dead `:45-49`; calls Financials "Billing" — a fourth name). Also zero-import: `components/ai/AIBriefingModal.tsx`, `components/ai/AiQuickActionsModal.tsx`, `components/ui/MiniCalendar.tsx`, `components/ui/DynamicHeaderWidget.tsx` (0 bytes), `components/RiskDashboard.tsx`, `pages/ClientList.tsx` (orphan page).

### 1b-CRITICAL. Live paths that still reach trial-hidden routes

`config/trialMode.ts:8-61` hides `/reporting`, `/document-intelligence`, `/video-sessions`, `/communication-center`, `/program-compliance`, `/portal/recovery-plan`, `/compliance`, `/session`. Three live leaks:

1. **Command Palette → "Messages"** (`components/ui/CommandPalette.tsx:38`) navigates to `/communication-center` → silent redirect to `/dashboard` (`App.tsx:129-131`). The palette imports no trialMode filter at all. Reachable via Ctrl/Cmd+K (`MainLayout.tsx:112-115`) and the header search bar (`GlobalHeader.tsx:56`).
2. **Command Palette → EVERY client row** (`CommandPalette.tsx:39-44`): every "View {name}" result navigates to `/program-compliance/${client.id}` — hidden (`trialMode.ts:26`) → redirect to `/dashboard` (`App.tsx:158-160`). **The primary global search returns N dead results — the single largest broken surface.** Should navigate to `/clients/${client.id}`.
3. **Clara's `navigate_to_page` tool** (`contexts/ClaraContext.tsx:239`, executed `:359` voice / `:541` text) is a raw `navigate(path)` with **no allowlist and no isTrialHidden check**, while her staff prompt (`:88-110`) primes her toward exactly the hidden surfaces. She confidently announces a navigation; the user lands on `/dashboard`.

Broken but currently unreachable (orphans — delete, don't repair): `ClientList.tsx:318`, `AIBriefingModal.tsx:31`, `BottomNavBar.tsx:27`, `VideoSessions.tsx:88` (targets `/video-sessions/:id/green-room`, a route that doesn't exist in App.tsx at all), `ProgressTracking.tsx:151`.

Correctly gated (verified, no action): `ClientProfileHeader.tsx:95,243`; `SessionManagement.tsx:29,60,583`; `PortalDashboard.tsx:320-321`; sidebar `:35,75` + drawer `:67,75`; `Dashboard.tsx:161` → `/session/:aptId/green-room` is the LIVE GreenRoom route, not hidden.

### 1c. Dashboard priority

Four stat cards render **Director-only** (`Dashboard.tsx:317`):

| Card | Value | Click |
|---|---|---|
| "Sessions today" `:322` | `appointments.length` | **none — dead number** (`:320-323`) |
| "Open alerts" `:330` | `alertSummary.total` | `/risk-monitor` |
| "Guardrail flags" `:338` | `guardrails.length` | in-page scroll to `#dashboard-guardrails` |
| "Active clients" `:343` | count ('—' if null — but see S3-2: coerced to 0 on failure) | **none — dead number** |

**The duplication problem:** `alertSummary.total` renders **three times** on one screen (red pill `:307`, stat card `:329`, Risk Monitor card button `:428`) plus its per-tier breakdown a **fourth** (`:407-422`) — all four pointing at `/risk-monitor`. `guardrails.length` renders twice (`:205`, `:337`). The comment at `:312-316` claims the stat strip "kills the duplicate treatment" — it added a third.

**Proposed hierarchy — the ONE number:** the **violation count** (`guardrails.filter(g => g.status==='violation').length`) — the only number on the screen that is (a) a regulatory exposure, (b) computed deterministically from real data, (c) actionable today. It is currently never rendered anywhere — only the undifferentiated `guardrails.length` total. Second tier: alerts total (work queue) + intake queue depth with age. Third tier: sessions today (glanceable schedule fact — belongs in the Schedule card header, not a stat card). "Active clients" is a vanity metric at 21 and a meaningless one at 100 — demote to Settings/Reports or the sidebar footer. The Risk Monitor card (`:405-431`) should collapse into the stat card it duplicates.

### 1d. Clara's summary line

**It is not AI.** Inline deterministic template in `Dashboard.tsx:139-152` (rendered `:299`), deliberately — the comment at `:127-130`: *"No AI generates these numbers — a hallucinated count in a clinician's morning is unacceptable."* Attributed to Clara visually (avatar `:296`, eyebrow `:298`) but she never sees it.

**Current template inputs:** `firstName`, `todayCount`, `flaggedClients` (distinct clientIds in the ALERT feed, `:134`), `recentComms.length` (Admin), `alertSummary.total` + `metrics.activeClients` (Director). Realistic Director output: *"No sessions are on today's schedule, with 0 open alerts across the caseload, and 12 active clients."* — three counts, two repeated verbatim in the stat strip 40px below, zero telling David where to look first.

**Already loaded in Dashboard state at render, zero extra fetch:**
- `guardrails: GuardrailVerdict[]` (`:52`) — count never spoken; each row carries `status`/`clientName`/`program`/`headline`/`detail`/`citation` (`complianceEngine.ts:268-278`). **The violation-vs-warning split is derivable and is the most decision-relevant number on the page.**
- `alerts: ClientAlert[]` (`:49`) — full objects, already sorted CRITICAL-first (`alertsService.ts:186-189`), so `alerts[0]` IS the recommended first action. `reason==='DEADLINE_IMMINENT'` rows carry "Deadline in N days" headlines (`:135`, CRITICAL at ≤3 days `:127`); `MISSED_SESSIONS` rows carry "N consecutive absences" (`:118`).
- `prospects` (`:55`) with `createdAt` (`api.ts:263`) — **oldest-intake age is one subtraction** — and `intakeFeePaid` (`:264`).
- `appointments` full objects (`startTime`, `clientName`) — "first at 9:00 with…".

**Requires new queries (not free):** practice-wide unsigned-notes count (no service exists; `clinical_notes.is_signed` only read per-client in `greenRoom.ts:83-92`); deadline-clock roll-up (`complianceClock.ts:160` is per-client); `counts.not_enforceable` (only `/compliance-readiness` fetches it).

## SECTION 2 — DATA PRESENTATION

### Formatter/label-map inventory (what already exists)

| Exists | Where | Missing |
|---|---|---|
| `PROGRAM_LABELS` / `programLabel()` / `normalizeProgram()` | `config/programVocab.ts:70-94` | — |
| `CLIENT_TYPE_LABELS` / `clientTypeLabel()` | `config/clientType.ts:44-64` | — |
| `SUBMISSION_STATUS_LABELS` + normalizer | `config/formSubmissionStatus.ts:25-58` | — |
| `formatTime12` etc. | `config/time.ts:34-54` | time-of-day only |
| Currency formatter | **none shared** — 6 private identical copies | `Intl.NumberFormat`: **0 hits repo-wide** |
| Date/relative-time formatter | **none exists** (no utils/ or lib/ dir) | `formatDate`, `daysAgo` |

### 2a. Raw values leaking to UI

**`program_type` raw despite `programLabel()` existing — 12 leaks.** Worst first:

- `pages/Dashboard.tsx:225` + `pages/ComplianceReadiness.tsx:79` — Guardrail rows render `{g.status} · {g.clientName} · {g.program}`: raw `OPIOID_RECOVERY`/`ANGER_MANAGEMENT`/etc. AND the raw lowercase verdict token, CSS-uppercased. The two most visible clinical surfaces. One-line fixes.
- `pages/RiskMonitor.tsx:29-36` — a SECOND private `formatProgram` whose acronym list omits CIP/OEP/WIP, so they title-case to **"Cip" / "Oep" / "Wip"** (`:127`). Live bug, not a preference. Delete in favor of `programLabel()`.
- `hooks/usePortalClient.ts:14-20` — a FOURTH shadow map with only 5 keys (including non-canonical CSTAR/REACT), falling through to raw tokens for 6 of 9 programs — on the **client-facing portal** (`:30`).
- `services/greenRoom.ts:70-77` — third implementation; passes raw column through ("OPIOID_RECOVERY · placement pending", plus the "SROP · Level IV (SROP)" stutter) → `GreenRoom.tsx:165,299`.
- `components/clients/ClientProfileHeader.tsx:69,82` — private switch re-implementation, raw canonical fallback.
- Also raw: `ClientWorkspace.tsx:327` (into Clara's prompt), `CommunicationCenter.tsx:68`, `CustomizeTreatmentPlanModal.tsx:297`, `ScheduleSessionModal.tsx:447` (raw `program` + `session_kind`), `:453` (raw `service_type` accrual token), `alertsService.ts:154-163` ("…hours into their **OPIOID_RECOVERY** program" in alert prose).
- Correct precedents to copy: `ClientSelectionGrid.tsx:18,101,236`, `ClientTypeBadge.tsx:36,39`, `EditClientModal.tsx:10`.

**`client_type` raw:** filter chips in `ClientSelectionGrid.tsx:554-565` render `RELAPSE_PREVENTION`/`DWI_COURT` raw (`clientTypeLabel` is imported in a sibling file of the same directory); `ScheduleSessionModal.tsx:465` same in the funnel select.

**Other enum leaks:** `ClientOverviewTab.tsx:420` raw payment status ("SUCCEEDED"); `:240` raw `ComplianceEvent.status`; `ComplianceReadiness.tsx:108` raw engine `Primitive` token; `:50` internal pack id shown to the Director; `ClientSubmissionsPanel.tsx:291` renders **raw form-JSON keys** (`clientSignature`, `previousSatop`) as field labels via `key.replace(/_/g,' ')` — real `label` strings exist in the form definitions and are bypassed; lowercase AI tags (`dui`) verbatim at `DocumentIntelligence.tsx:383` + `ClientFormsTab.tsx:101` (source `storageService.ts:78-88`, Gemini free-text, net-new normalizer needed).

**Timestamps:** `Dashboard.tsx:167-168` renders raw 24-hour `"18:00"` on Today's Schedule while every other schedule surface uses `formatTime12` (`SessionManagement.tsx:506`, `scheduleLane.tsx:212`, `AppointmentStatusModal.tsx:296`) — same data, two formats, same screen family. Also raw at `ClientSessionsTab.tsx:203`, `CalendarView.tsx:81`. Raw ISO `YYYY-MM-DD` shown to users: `ComplianceDeadlineStrip.tsx:51,79-80`, `AssessmentTab.tsx:621`, and embedded in clock prose (`complianceClock.ts:107-114,168-169`). Date granularity (toLocaleString vs toLocaleDateString) is per-call-site with no rule — ~15 sites split roughly evenly.

**Currency:** six byte-identical private formatters (`Financials.tsx:16`, `BillingLedger.tsx:55`, `RecordPaymentModal.tsx:37`, `paymentReceipt.ts:54`, `complianceClock.ts:89`, `ClientOverviewTab.tsx:134-137`) — all `$${n.toFixed(2)}`, no thousands separators. Inline unformatted on documents that leave the building: `CourtReportPreview.tsx:76-77` (court), `SuperbillPreview.tsx:68,78` (insurer), `PortalBilling.tsx:149` (client). `AppointmentStatusModal.tsx:362` has no `.toFixed` at all ($25 not $25.00). `$1234.50` should read `$1,234.50`.

**Ids:** no full UUIDs render to users (good). `ClientOverviewTab.tsx:426-427` shows truncated Stripe `pi_…` ids in mono — opaque but bounded. `BaseFormTemplate.tsx:265` `ACS-XXXXXXXX` confirmation code is the deliberate good pattern.

### 2b. Pluralization

A correct `plural()` helper exists — **local and unexported** in `pages/Dashboard.tsx:16`, used correctly seven times in the same file. Meanwhile:

- **`Dashboard.tsx:321-322` — "1 SESSIONS TODAY" (the reported bug), plus "OPEN ALERTS" `:330` and "ACTIVE CLIENTS" `:343` — while the fourth tile in the same row pluralizes correctly (`:338`).** Inconsistent within one 4-tile strip.
- Also broken at n=1: `FormLibrary.tsx:216` ("1 forms available"), `GreenRoom.tsx:173-175` ("1 hrs / 1 counseling / 1 days to go"), `ClientOverviewTab.tsx:380` ("1 Days"), `AssessmentTab.tsx:621` ("(1 days remaining)").
- The `(s)` escape hatch ×4: `complianceClock.ts:111,168`, `complianceEngine.ts:739`, `AssignFormModal.tsx:110`.
- `MobileDocumentUpload.tsx:450` uses `> 1` instead of `!== 1` — renders "0 field need review".
- Zero states: most cards have real empty states (good list in Section 3), but three of four Director stat tiles render bare `0` with no way to distinguish "0" from "query failed" (only Active clients has the `—` null path, `:342` — and see S3-2 for why even that is broken).

### 2c. Aging and time

**Exactly one aging signal exists in the entire app**: `daysSinceEnrollment` (`greenRoom.ts:167` → `GreenRoom.tsx:157,175,194`, with a proper progress bar), duplicated privately as `computeDaysSince` (`ClientOverviewTab.tsx:145-152` → "Days in Program" card `:387`). Everything else renders absolute dates with no elapsed-time signal:

| Surface | Data available? | Displayed? |
|---|---|---|
| Intake queue (`Dashboard.tsx:268`) | `createdAt` fetched and rendered as bare grey date (`api.ts:272,291`) | **No age, no threshold highlight** — the 6/30 intake looks identical to this morning's; only visual state is the fee pill (`:276-278`) |
| Unsigned notes | `created_at` selected alongside `is_signed` everywhere (`ClientSessionsTab.tsx:241`, `ClientOverviewTab.tsx:187`, `greenRoom.ts:83`); GreenRoom's API **returns `createdAt` and the UI drops it** (`greenRoom.ts:92`) | **"unsigned for N days" appears nowhere** (grep: 0 hits). Note: no `signed_at` column exists (`ClinicalNoteView.tsx:12`), so days-since-signing is unknowable — days-since-creation is free |
| Treatment plan review | Plan `createdAt` reaches the compliance engine (`ClientWorkspace.tsx:206`) and the profile header renders "Plan Review: Overdue / {N}d" — **for non-SATOP only** (`ClientProfileHeader.tsx:116-130`) | The Treatment Plan tab itself shows no due date or countdown (`TreatmentPlanTab.tsx:140-141,254`). The alerts-service 90-day rule fires on **hours accrued**, not plan age (`alertsService.ts:152-167`) |
| Screening/deadline clock | `complianceClock.ts` computes signed `daysRemaining`, negative after elapse (`:56,151`) — strongest module in this section | `ComplianceDeadlineStrip.tsx:76-85` **drops the magnitude once negative** — a window blown 2 days ago and 90 days ago render identically |
| Guardrail flags | **No timestamp exists** — `GuardrailVerdict` has no `computedAt`/`raisedAt` (`complianceEngine.ts:268-278`); recomputed each mount, never persisted | Schema gap, not display gap — scope separately. (`ClientAlert.computedAt` exists but equals fetch time — displaying it would mislead) |

The countdown pattern done right already exists at `ClientOverviewTab.tsx:236` ("N days / N days overdue") — correct pattern, wrong surface.

### 2d. Test data on production surfaces

- **The `is_demo` column exists** (`supabase/migrations/20260605_completion_gate_columns.sql:22`) and is used **only for PDF watermarking** (`cimorPacket.ts:117-121`, `paymentReceipt.ts:133-138`, `BillingLedger.tsx:128,137`). **Zero list queries filter on it** — not `getClients` (`api.ts:215-234`, whose docblock self-describes as "the single choke-point"), not `getProspects` (`:269-294`), not `getAppointments` (`:452-470`), not `fetchAlerts` (`alertsService.ts:211-214`), not `fetchComplianceGuardrails` (`complianceEngine.ts:467-505`).
- Today's Schedule rows are the `dee0…` seed namespace (`20260714_demo_week_seed_v2_full_week.sql:40`), joined to fixture-client namespaces (`aaaaaaaa-…`, `ffffffff-…`, etc. — Marcus Reyes, Derek Flower, …).
- **"Joe Blow" and "FLOWER TESTER" are NOT in the repo** — the only mention is a comment in `scripts/formIntegrityCheck.tsx:21`. They are live rows created through `PublicIntake.tsx` (which hardcodes `status='prospect'`, docblock `:12`) with `is_demo` at its default `false`. **No id-prefix filter can catch them.**
- Recommendation (no data touched in this recon): (1) add `includeDemo?: boolean` default-false to `getClients` + mirror in the three queries that bypass the choke-point — **S**; (2) prerequisite data step, flagged not done: reversible migration backfilling `is_demo=true` on fixture namespaces + the live junk prospects — the only mechanism that catches Joe Blow; (3) appointments have no `is_demo` and `client_id` is TEXT/no-FK (SECURITY_BACKLOG #7), so cheapest correct filter is `.not('id','like','dee0%')` behind the same flag; (4) alternative for demo-heavy environments: a `SAMPLE` chip reusing the exact vocabulary the PDF watermark already established.

---

## SECTION 3 — THE UNGLAMOROUS 80% (states audit, top ~10 screens)

**Routing corrections discovered during the audit** (these change what "the top screens" even are):
- `pages/Calendar.tsx` is a **0-byte file**, not imported. The calendar is `pages/SessionManagement.tsx` (`App.tsx:132`).
- `pages/ClientList.tsx` is an orphan (self-declared at `ClientList.tsx:15`). The real client list is `components/clients/ClientSelectionGrid.tsx`, rendered by `ClientWorkspace` at zero-clientId (`ClientWorkspace.tsx:308`).
- `pages/FormSubmissions.tsx` is a 12-line deprecated stub rendering `<h1>Deprecated Content</h1>` (`:7`), not routed. The real queue is `components/reports/ClientSubmissionsPanel.tsx` (mounted `pages/Forms.tsx:156`).
- The staff intake queue lives inside the Dashboard (`pages/Dashboard.tsx:250-287`), not on its own page.

### Per-screen matrix

| Screen | Empty | Loading | Error | Destructive confirm | Validation | Long content / 100-client |
|---|---|---|---|---|---|---|
| Dashboard | designed per card | `DashboardSkeleton` (only skeleton in app) | **FAIL-OPEN ×4** | n/a | n/a | schedule + intake maps unbounded |
| ClientSelectionGrid (`/clients`) | bare one-liner | `LoadingSpinner` | good (visible + retry) | `window.confirm` ×2, best copy in repo | n/a | **N+1: ~140 requests at 60 clients** |
| ClientWorkspace (`/clients/:id`) | `Client not found.` unstyled | `LoadingSpinner` (**load-bearing — do not touch**) | per-section fallback + retry (forms/docs only) | none in page | n/a | `text-4xl` name, no truncate |
| SessionManagement (calendar) | **none — naked grid chrome at zero** | `LoadingSpinner`, full flash per week-arrow | **best in repo** (`:47` "FAIL VISIBLY", `:358-369` retry) | `window.confirm` ×3 | modal-side | O(n²) conflicts OK; unbounded date-unbounded fetch is the 2-yr problem |
| ActiveSession | designed | **unstyled `<div>Loading session...</div>`** | **none — hangs forever on reject** | modal step (correct) | n/a | fine |
| Forms + ClientSubmissionsPanel | designed | `LoadingSpinner` | **FAIL-OPEN** (error never destructured) | none | on-submit only, `'Required.'` | unbounded `select('*')` → ~1,400 rows at scale |
| PublicIntake | n/a | inline spinner | honest 3-state payment verify (strong) | n/a | **on-submit, 2 of 4 fields, banner off-screen on mobile** | fine |
| Financials | designed ×2 (unreachable on error — correct) | per-section `Spin` (best loading model) | **exemplary — the model to copy** | n/a | n/a | holds; names push via overflow-x |
| ComplianceReadiness | designed (green) | text line | **FAIL-OPEN — CONFIRMED STILL LIVE** | n/a | n/a | unbounded flags list |
| Reporting | none (impossible — data is hardcoded) | `LoadingSpinner` | none (would hang) | n/a | n/a | **mock data** |
| Portal Dashboard | designed | `LoadingSpinner` | **FAIL-OPEN + misdiagnosed as "Session Expired."** | n/a | n/a | `.limit(3)` |
| Portal Documents / Billing / Appointments / Compliance | designed (Appointments "Past" missing) | mixed | **exemplary** (`PortalErrorCard` + explicit error checks) | n/a | n/a | mostly bounded |

### Worst offenders (full detail)

**S3-1. `/compliance-readiness` renders a green all-clear when the query fails — prior finding UNFIXED.**
`services/complianceEngine.ts:530-548`: `fetchComplianceReadiness` builds a fully-formed all-zeros `base` object and returns it on any query error (`:547`), never throwing. So the page guard `if (!data)` at `pages/ComplianceReadiness.tsx:38-40` is dead code for query failures. On RLS denial / network drop / expired session, a Director-only regulatory screen renders `pages/ComplianceReadiness.tsx:88-91` in emerald with a checkmark: **"No active flags — every enforceable rule is met."**, plus "across 0 active clients" (`:49`), "Nothing pending" (`:115`), "Verdicts are deterministic" (`:121`). Only signal: `console.warn`.
Same root feeds the Dashboard: `complianceEngine.ts:467-476` `fetchComplianceGuardrails` returns `[]` on error → `Dashboard.tsx:241` "No compliance flags." Third instance: `fetchClientAccrual` returns `ZERO_ACCRUAL` on error (`complianceEngine.ts:322-330`) — fails closed for the cert gate (correct) but renders an honest-looking "0 hrs completed."
*Good:* engine functions throw on error (or return a tagged failure) and pages render the existing error-card pattern (`PortalErrorCard` / Financials' `SectionError`). **Effort: S.** Holds at 100 clients: failure severity *grows* with caseload.

**S3-2. Dashboard fail-open ×4 + count coercion.**
`pages/Dashboard.tsx:83, 88, 96, 105` — four `console.warn`-only catches leaving reassuring empties: Clara's briefing says "no clients are currently flagged" (`:146`), guardrails card "No compliance flags." (`:241`), intake queue "No new intakes." (`:283` — a paid prospect becomes invisible), messages (`:388`). Also `:112`: active-client count never destructures `error`; supabase-js doesn't throw, so the `catch` at `:113` is unreachable and the tile shows **"0 Active clients"** on RLS denial — directly contradicting the code's own comment at `:57` ("null = query failed → render '—'"). **Effort: S** (destructure error, set a per-card error flag, render the existing error style).

**S3-3. `pages/Reporting.tsx` charts hardcoded fake data.**
`services/api.ts:1626-1640`: `getRevenueData` and `getComplianceTrendData` return literal arrays — $12,500 SATOP revenue, compliance trend 88%→98%, no DB involvement. A Director report showing fabricated money. Currently behind `isTrialHidden('/reporting')` (`App.tsx:188-189`). No error path (`:21-28` no try/catch — would hang the spinner, impossible today only because the functions can't fail). *Good:* either wire to the real `acs_report_*` RPCs that Financials already uses, or keep it hidden. **Effort: M** to wire real data; XS to verify it stays hidden.

**S3-4. Staff form-review queue shows a clean empty inbox on failure.**
`components/reports/ClientSubmissionsPanel.tsx:36-44` destructures only `{ data }` on both queries; catch at `:53-55` warns only. Failure renders summary tiles **0 Awaiting / 0 Needs Review** (`:133,145`) over an empty table. (The approve path, by contrast, is correct and well-commented `:66-77`.) Also unbounded `select('*')` (`:38`) + unbounded `filtered.map` (`:203`) → ~1,400 rows at 100 clients × 14 registry forms. **Effort: S** for the error check; S for a limit + "load more".

**S3-5. Portal dashboard misreports failure as "Session Expired."**
`pages/portal/PortalDashboard.tsx:178-199` — three unchecked `{ data }` destructures (the exact anti-pattern its sibling files document against: `PortalAppointments.tsx:24`, `PortalDocuments.tsx:169`, `PortalBilling.tsx:30`); on failure `:295` shows "Pending Forms: 0", `:311` "No upcoming appointments.", and the null path hits `:218` → **"Session Expired."** — wrong diagnosis, no retry, sends the client to a re-login that won't help. `PortalErrorCard` already exists for exactly this. **Effort: XS–S.**

**S3-6. Calendar has no empty state.**
`SessionManagement.tsx:419-561` — zero appointments renders naked grid chrome (day columns, hour lines, red now-line) over nothing. Failed `getCounselors()` falls back to `[]` (`:238`, deliberate soft-fail) → `CounselorDayView.tsx:65` computes `repeat(0, …)` → a lone time gutter beside a blank panel, no message. **Effort: S.**

**S3-7. ActiveSession can hang forever on a clinical screen.**
`pages/ActiveSession.tsx:33-39` — no try/catch around `getClient`; a rejection strands the unstyled `<div>Loading session...</div>` (`:138`). SOAP-generation failure is written INTO the editable note body (`:127` `setNote("Error: Could not generate SOAP note...")`) — error text becomes saveable clinical content. **Effort: XS–S.**

**S3-8. ClientSelectionGrid N+1 request storm.**
`components/clients/ClientSelectionGrid.tsx:87` — each non-SATOP card fires its own `fetchClientProgramCardState`; eligibility effect fires 4 queries per candidate (`:341-368`, `:352-357`). At 60 non-SATOP clients + 20 candidates: **~140 requests to paint one page.** No pagination/virtualization (`:576`, `:218`). Cards also pop in staggered ("Checking compliance…" `:120-121`). **Does not hold at 100 clients on request volume** (visually it survives). **Effort: M** (batch into one query keyed by client ids).

**S3-9. Long client names break the three most visible name renders.**
`components/clients/ClientProfileHeader.tsx:150` — `<h1 className="text-4xl font-black tracking-tighter">` no truncate/max-width (most visible break in app). `Dashboard.tsx:172` — schedule-row `<h4 className="font-bold text-lg">` no truncate, collides with the chevron (`ArrowUpRight`). `ClientSelectionGrid.tsx:100` — card `<h3>` wraps to 3 lines, unbalances the grid row. (Counter-examples that do it right, same files: `Dashboard.tsx:267,381` use `truncate` + `min-w-0`.) **Effort: XS.**

**S3-10. PublicIntake validation is thin at the unauthenticated front door.**
`pages/PublicIntake.tsx:91-92` — on-submit only, 2 of 4 fields; phone accepts `"x"`; email validated by neither JS nor browser (no `required`, form not `noValidate`, `:276`); single top-of-form banner (`:260-265`) that is **off-screen on mobile** when the user taps submit at `:288`; no scroll-to-error (BaseFormTemplate has one — `:186-189` — this page doesn't); no `aria-invalid`/`aria-required`; second error overwrites the first. The *payment* honesty design in the same file is strong (`:24`, `:49-84`, three honest return states `:138/:155/:192`) — keep it. **Effort: S.**

### Other findings (states)

- **Empty states, undesigned:** ClientSelectionGrid `:594-598` bare one-liner, no "no clients yet" vs "filter excluded them" distinction, Add Client CTA 500px away in header (`:489`); ClientWorkspace `:310` `Client not found.` with no back-link; Portal Appointments "Past Sessions" heading over nothing (`PortalAppointments.tsx:107-127` — `past.map :110` has no length check, while "Upcoming" gets a designed empty `:99-103`).
- **Portal past appointments hardcode "Completed"** on every row regardless of `appt.status` (`PortalAppointments.tsx:121-123`) — a no-show displays to the client as completed.
- **Portal Billing dead buttons:** "Update Method" (`:154`), "Download Tax Statement"/"Insurance Claims" (`:161-168`) have no handlers — fabricated affordances of the class the portal honesty pass removed elsewhere.
- **Skeletons:** `components/skeletons/` contains exactly one file (`DashboardSkeleton.tsx`), a generic 4-block pulse that doesn't match the real layout → shift on resolve (`Dashboard.tsx:125`). Everything else is `LoadingSpinner`/`PageLoader` or plain text.
- **Destructive confirms are all native `window.confirm`** (SessionManagement `:126,194,212`; ClientSelectionGrid `:393`; TreatmentPlanTab `:61`; `FileManager.tsx:82` bare `confirm` + uninformative `alert("Deletion failed.")` `:87`). Copy quality varies from excellent (ClientSelectionGrid `:390-392` explains reversibility; SmartNoteImporter `:95` "Signed notes are permanent…") to bare. Failure paths are `alert()` throughout.
- **Form validation** (`components/BaseFormTemplate.tsx:173-189` + `config/formValidation.ts`): on-submit only, generic message literally `'Required.'` (`formValidation.ts:72`), no error summary/count → fix-one-resubmit-discover-next loop on 30-field forms. The validation *logic* is careful (`:37-40` answered-not-true, `:52-67` object case); the *messaging* is the gap. Model error message exists at `BaseFormTemplate.tsx:194-197`.
- **`ClientOverviewTab.tsx:211-213`** — billing query catch → warn only → "No payments on file." (`:400`) on failure.
- **ClientWorkspace partial degradation gap:** `Promise.allSettled` (`:224-254`) gives fallbacks+retry to forms/documents only; a failed `fetchClientAccrual` → `undefined` (`:248`), failed determination → `null` (`:249`) flow silently into `assessClient` (`:315`) and change the completion verdict with no UI signal.
- **SessionManagement date-unbounded fetch:** `getAppointments()` pulls ALL appointments ever (`:236`) and filters client-side (`:466`). Fine today, grows forever.

### Positive patterns to propagate (feeds Section 5)

- `pages/Financials.tsx:60-64,96,117,132` — explicit `error` destructure → `SectionError` **instead of** the table; empty state unreachable on error; self-auditing totals strip (`:169-177`) that turns rose when arithmetic doesn't reconcile.
- `pages/portal/PortalDocuments.tsx:176,191-202` — throw + early-return error card before any derived state; comment names the hazard ("A failed load must NEVER render as 'everything is pending'").
- `components/ui/PortalErrorCard.tsx` — shared error component whose docblock states the rule: "An empty state must mean 'really empty', never 'the load failed'."
- `SessionManagement.tsx:47,358-369` — "Calendar load must FAIL VISIBLY — never silently render phantom/mock appointments" + dedicated retry panel.
- `ClientSelectionGrid.tsx:390-392` — destructive-confirm copy explaining reversibility and retention.
- `PublicIntake.tsx:24,49-84` — server-verified payment states; "never a false 'confirmed', never a false 'failed'."

---

## SECTION 4 — CRAFT

### 4a. Type scale, weight, spacing, radius

- **Font size: ~45 distinct values.** Tailwind scale 10 values / 1,155 uses; arbitrary `text-[Npx]` 9 values / **414 uses** (`text-[10px]` ×253, `text-[11px]` ×78, `text-[9px]` ×39, down to `text-[8px]` ×5 and half-pixel `text-[13.5px]`/`text-[12.5px]` — pure nudging); inline `fontSize` ~25 values / 40 uses. 79% of sized text is `text-sm` or smaller. The arbitrary tier is a second, unofficial scale living below `text-xs`. Worst files: `AssessmentTab.tsx` (47), `GreenRoom.tsx` (24), `DocumentIntelligence.tsx` (24), `Dashboard.tsx` (20). The one custom config token (`text-h1`, `index.html:29-31`) has exactly one consumer (`Header.tsx:22`). `pages/WebsitePortalBridge.tsx` is a separate universe: 97 inline style blocks, 40 rem font sizes, participates in no scale.
- **Font weight: everything is shouting.** `font-bold` 517 + `font-black` 472 = **76% of all weight declarations**; `font-normal`/`font-light` total 8 uses across 250 files. `font-black` (900) at `text-[10px] uppercase tracking-widest` (e.g. `TreatmentPlanLibrary.tsx:89`) is a rendering-quality problem as well as a hierarchy one. This is the loudest craft signal in the codebase — when everything is bold, nothing is.
- **Spacing: genuinely clean.** 2 arbitrary values repo-wide (`mt-[-1.5rem]`, `mr-[420px]`). Leave it alone.
- **Radius: four competing languages.** `rounded-xl` 256 / `rounded-full` 237 / `rounded-2xl` 229 / `rounded-lg` 154 / `rounded-3xl` 36, plus 20 arbitrary sites (`rounded-[2.5rem]` etc.) and one `rounded-[15px]` (`Compliance.tsx:21`) that is 1px off `rounded-2xl`.
- **Verdict:** ad hoc except spacing. Type size + weight is the highest-leverage craft fix.

### 4b. Tokens — the Attesta answer: hundred-file sweep today, one-file edit if you pull the right lever

- **Theming lives in exactly one fragile place**: the inline `tailwind.config` in `index.html:24-127`, executed against the **Tailwind Play CDN** (`:23`). No `tailwind.config.js` exists. **No global CSS file exists — `index.html:128` links `/index.css`, which is a 404 in dev and in `dist/`.** The only real CSS file is `pages/help/help.css`. Play CDN in prod = no purge, no build validation, and custom `@keyframes` "can silently no-op" (the team's own comment, `index.html:109-111`).
- **Tokenized:** `primary` ramp (#8B1E24), `surface`/`background`/`border`/`hairline` + dark mirrors, card shadows, and the **`slate.400/500` override** (`index.html:38-44`) whose comment states its purpose: move ~625 raw slate sites at once "WITHOUT a per-component sweep." The team already hit this wall once and tunneled under it — that's the proven lever.
- **The ratio: 1,060 semantic-token uses vs 4,812 raw palette uses (1:4.5 against).** Every status color — amber warning (329), red/rose risk (523), emerald/green compliance (415), blue/indigo info (266) — is hardcoded to a Tailwind hue. **No success/warning/danger/info token exists.**
- **122 dead-token class uses across 22 files** compile to nothing: `text-on-surface-secondary` ×78, `text-on-surface` ×20, `text-surface-secondary-content` ×17, `text-dark-surface-secondary-content` ×6, `bg-on-surface-secondary` ×1. (DEFERRED #24 recorded 431× on 7/15 for the first of these — count methodology differs; the fix is identical: define them in the colors block, then verify.) These sites **never received the contrast raise** — they inherit parent color. Live examples: `CommandPalette.tsx:64,83-94`, `Header.tsx:23`, `SignaturePad.tsx`, `layouts/PortalLayout.tsx`, `Settings.tsx`.
- **Raw hex is modest**: 84 occurrences, ~40 values — but the **brand color bypasses its own token in 5 places** (`VisualAuditPanel.tsx:96` hardcodes both primary AND primary-focus; `IValtMfaModal.tsx:95`; `GlobalHeader.tsx:142` bakes `8B1E24` into a ui-avatars.com URL), and an undeclared success-green `#1F7A4D` recurs as a pseudo-token in 3 files.
- **Portability verdict**: portable today = the maroon ramp + shadows + slate-400/500 tone (one-file). NOT portable = 4,812 raw palette sites, 2,738 slate sites (only 400/500 overridable), 122 dead tokens, WebsitePortalBridge. **Extending the slate-override trick to amber/emerald/red/rose/blue + adding semantic tokens converts ~1,500 sites into a one-file edit** — the single most important move for Attesta parity.

### 4c. Interactive states

- **There is no Button primitive and no Input primitive.** `components/ui/` provides chrome, Card, Modal, logos, spinners — zero form/action primitives. 389 `<button>` and 102 `<input>` elements, each styled at the call site.
- **136 distinct className strings contain `bg-primary`** — 136 spellings of "the primary button," disagreeing on radius (`rounded-md`→`rounded-2xl`), padding, weight (`font-medium`→`font-black`), size (`text-[10px]`→`text-sm`), focus ring (mostly absent), and disabled treatment. Sample: `ClientWorkspace.tsx:508` vs `Login.tsx:220` vs `ClientLogin.tsx:118` vs `PublicIntake.tsx:288` vs `BillingLedger.tsx:250`.
- **State coverage:** `hover:` 762 vs `focus:` 221 (3.4:1 — hundreds of mouse affordances with no keyboard one), `active:` on 10% of buttons, `disabled:` implemented as **15 competing recipes** (opacity-50/60/70/40, bg-gray-400, cursor variants…). Focus rings disagree: ring-2/ring-4/ring-1/ring-0; ring-primary vs ring-indigo-500 vs ring-amber-500. Only 58 of 389 buttons use the `bg-primary`+`hover:bg-primary-focus` token pair — but that pair is a reliable codemod anchor for a future `ui/Button.tsx`.
- `Card.tsx` is good but hardcodes `bg-white dark:bg-slate-800` (`:19`) instead of the `surface` token that exists for exactly this.

### 4d. Keyboard + a11y

- **`focus-visible:` = 0 occurrences app-wide**, and there is no stylesheet to hold a global focus style (see the `/index.css` 404). `focus:` styles fire on mouse click too, so sites defensively add `focus:outline-none` — where that lands without a replacement ring, the element is invisible to keyboard users (`CommandPalette.tsx:71` — the search input itself).
- **30 clickable `<div>/<span>/<li>` across 21 files; `tabIndex` appears 4× repo-wide.** The systemic one is `Card.tsx:16-24`: accepts `onClick`, applies `cursor-pointer`, renders a bare div — every clickable card inherits keyboard-invisibility. `role="button"` appears 4× total.
- **`Modal.tsx` (the shared modal): has `role="dialog"`+`aria-modal`, lacks everything else** — no focus trap, no Escape (the global handler at `MainLayout.tsx:116-125` closes a hardcoded list; `EditClientModal` and `CustomizeTreatmentPlanModal` rendered at `:231,242` cannot be closed with Escape at all), no focus restore, no `aria-labelledby` (title `<h2>` has no id), unlabeled icon-only close X (`:30-32`), no backdrop-close (the inner `stopPropagation` guards a handler that doesn't exist). Also `if (!isOpen) return null` (`:14`) is not an unmount — the exact PHI-retention hazard the team documented and fixed for one call site (`MainLayout.tsx:214-226`) still lives in the primitive.
- **CommandPalette: no keyboard support at all** — no arrow keys, no Enter-to-select, no `role="listbox"`/`aria-activedescendant`; open with Ctrl+K, type, then reach for the mouse. Escape and backdrop-click do work.
- **Header dropdowns** (`GlobalHeader.tsx:86-104` create menu, `:145-161` profile): no `aria-haspopup`/`aria-expanded`/`role="menu"`, no Escape, **no click-outside dismiss** — stays open until re-clicked. The correct pattern already exists in-repo at `ClientWorkspace.tsx:291-294` (Escape + click-outside); it wasn't reused.
- **ARIA totals: 79 attributes for the whole app; 47 `aria-label` vs 389 buttons.** Best-annotated control: the Clara launcher (`GlobalHeader.tsx:115-116`, label + `aria-pressed`). Unlabeled: the shared Modal X, notifications bell (whose unread indicator is a color-only dot, `:131`), mobile search, CommunicationCenter's phone/video/attach/emoji row, Clara send button.
- **Forms: 102 inputs, 25 `htmlFor`, zero `id`s on inputs** — even the 25 labels are visual-only. Most inputs rely on placeholder as their sole label.
- **Contrast (beyond the prior raise, which is sound):** (1) the 122 dead-token sites never got the raise; (2) 297 sites of ≤10px text, frequently 900-weight on tinted backgrounds — below WCAG 1.4.4 territory regardless of measured ratio; (3) **`slate.400 = #627082` was tuned for cream and measures ~3.6:1 on the dark background `#020617`** — every `dark:text-slate-400` fails AA, including shared `Card.tsx:31`; (4) status is color-only in most of the ~1,500 amber/emerald/red sites (`Dashboard.tsx:194` comments on the "wall of red/amber" itself).
- Dead code found in passing (deletion candidates): `DynamicHeaderWidget.tsx` (0 bytes), `AIBriefingModal.tsx` (188 lines, 0 imports), `AiQuickActionsModal.tsx`, `MiniCalendar.tsx`, `RiskDashboard.tsx`, `BottomNavBar.tsx`, `pages/ClientList.tsx`, `pages/Calendar.tsx` (0 bytes), `pages/FormSubmissions.tsx` (deprecated stub).

### 4e. Header real estate

- **The search bar is a fake input** — a `<button>` wrapping a styled div reading "Search clinical resources..." (`GlobalHeader.tsx:55-63`) that opens the Command Palette. The palette's actual corpus (`CommandPalette.tsx:35-50`): **3 hardcoded page shortcuts + client names.** Substring match on display strings only ("vi" matches every "View {name}" row). Not searchable: notes, documents, appointments, forms, submissions, plans, billing, messages content, resources — **including the actual Resources page the placeholder implies.** And every client result is a dead link (Section 1b).
- **Real estate:** `flex-1 max-w-xl` centered (`:55`) → ~576px ≈ **40% of a 1440px header, ~55% of its free space** — the largest element in the chrome, mapping to a substring match over one entity. Hidden below 640px.
- **42 CFR note:** the palette lists every client name in the practice on a single Ctrl+K — worth weighing against constraint 3 before making search *more* prominent; the fix direction (search more entities, or shrink the bar) should not increase casual name exposure on an always-visible surface.
- **Clara instance count on /dashboard: 4 distinct surface types, 3+N simultaneous avatars** — header launcher with perpetual `animate-ping` ring (`GlobalHeader.tsx:112-127`), briefing-line avatar + "CLARA" eyebrow (`Dashboard.tsx:296-298`), "Ask Clara to explain this flag" per guardrail row (`:231-235` — N of them), docked panel header when open (`SynapseChatPopover.tsx:71-86`). With 5 flags: 8 Clara avatars on one screen, and the ping ring keeps pulsing after the panel is already open. (Credit: the team has been consolidating — the portal "Ask Clara" card was removed 7/27, `PortalDashboard.tsx:327`.)
- Also: the header shows the platform-vendor logo (`GemyndFlowLogo`, `GlobalHeader.tsx:4,51`) while `AcsTherapyHubLogo.tsx` exists — a third branding axis (vendor vs tenant) that matters for the Attesta fork. And `components/ui/Header.tsx` (the page-title header, 6 consumers) styles its subtitle with two dead tokens (`:23`) and an untokenized blue/purple/pink aurora gradient (`:14-19`) outside the maroon brand family.

### 4f. Print

- **Total print CSS: 6 utility uses in 2 files + 1 inline block.** Deliberately print-styled: `BaseFormTemplate.tsx:285,294,397,428` (correct — the pattern to follow), `ProgressBar.tsx:12`, and `MeetingSummary.tsx` — which uses a fragile DOM hack (`display:none` on `#root`, `setTimeout(500)`, **no try/finally** — a slow-dismissed dialog leaves the app blank, `:14-30`).
- **Court/state documents go through jsPDF and are well-built** — shared letterhead/footer/watermark kit (`services/pdfDocuments.ts`), CIMOR packet (`cimorPacket.ts:99-102`), receipts (`paymentReceipt.ts:111-113`), and the same-instance preview==save discipline (`DocumentPreviewModal.tsx:96,129`). No notes.
- **Everything else — 30+ routes — prints as garbage:** fixed blurred header overlaps page 1 (`GlobalHeader.tsx:37`), sidebar prints a 256px empty gutter on every sheet (`MainLayout.tsx:174`), open Clara panel prints OVER content (`SynapseChatPopover.tsx:67`), and **dark mode prints near-black full-bleed pages** (`index.html:130`, no `print:bg-white` anywhere). Likely-to-be-printed and unsupported: Financials/BillingLedger, ComplianceReadiness, TreatmentPlan, `CourtReportPreview.tsx`, and **`SuperbillPreview.tsx` — a document a client physically hands an insurer.**
- The fix is ~15 lines of global `@media print` (force light, hide chrome, drop shadows/blur, zero the sidebar margin) — **but it has nowhere to live until the missing `/index.css` is created.** Pair with finding #7.

---

## SECTION 5 — WHAT'S ALREADY GOOD (do not destroy in a refactor)

1. **The provenance discipline: deterministic verdicts + inline statute citations.** One verdict constructor (`complianceEngine.ts:108-115`), citations carried as data from `compliance/missouri-compliance-pack.json` and rendered in a consistent mono/slate treatment at six sites (`Dashboard.tsx:228`, `ComplianceReadiness.tsx:82,111`, `ClientOverviewTab.tsx:103,285`), violations sorted before warnings (`complianceEngine.ts:503,591`). The same instinct produced Clara's **deliberately non-AI briefing line** (`Dashboard.tsx:127-130`: "a hallucinated count in a clinician's morning is unacceptable") and the Financials **self-auditing totals strip** (`Financials.tsx:169-177`). This is the app's best idea. Extension direction: a shared `<Citation>` primitive (the className is currently copy-pasted) and severity tokens — never dilution. Two citation renders bypass the treatment entirely (`AssessmentTab.tsx:789`, `OutreachModal.tsx:32` — statute inside prose sent to a client).
2. **Fail-visibly error discipline — where it was applied.** `PortalErrorCard.tsx` and its docblock rule ("An empty state must mean 'really empty', never 'the load failed'"), the portal pages that use it, `Financials.tsx`'s per-section errors, `SessionManagement.tsx:47`'s "never silently render phantom appointments," `ClientSelectionGrid.tsx:326`'s "fails VISIBLY (no mock fallback)," and `PublicIntake`'s three honest payment states. Finding #1 is not a new idea — it's finishing the rollout of this one.
3. **The document pipeline.** jsPDF kit with shared letterhead/footer/`sampleWatermark`, letter geometry, `is_demo`→"DEMONSTRATION SAMPLE" watermarking, and preview-equals-output via same-instance blob (`DocumentPreviewModal.tsx:33-36,59`). Also the `ACS-XXXXXXXX` human confirmation code (`BaseFormTemplate.tsx:265`) and `BaseFormTemplate`'s print styling — the only screen that prints correctly on purpose.

Honorable mentions: the spacing scale (2 arbitrary values repo-wide — genuinely disciplined), the config/ label-map layer (`programVocab`/`clientType`/`formSubmissionStatus` — the fix for Section 2 is *using* them, not building them), destructive-confirm copy at `ClientSelectionGrid.tsx:390-392`, and the load-bearing-comment culture itself (`ROADMAP.md`, `sessionTaxonomy.ts:16-18`, `trialMode.ts`) — half this recon's findings were self-documented by the team.

---

## ONE DAY — maximum visible improvement in one focused day

Morning (mechanical, ~20 sites, all one-liners):
1. `programLabel()` on the Guardrails card + ComplianceReadiness (`Dashboard.tsx:225`, `ComplianceReadiness.tsx:79`) + a tiny WARNING/VIOLATION label map.
2. Delete `RiskMonitor.tsx:29-36` (`formatProgram`) → `programLabel()` — fixes "Cip/Oep/Wip".
3. Delete the portal shadow map (`usePortalClient.ts:14-20`) → import from `programVocab`.
4. Export `plural` from a shared module; apply to the four stat tiles (`Dashboard.tsx:321-343`) — kills "1 SESSIONS TODAY".
5. `formatTime12` on Today's Schedule (`Dashboard.tsx:167-168`).
6. `truncate min-w-0` / `max-w` on the three name renders (`ClientProfileHeader.tsx:150`, `Dashboard.tsx:172`, `ClientSelectionGrid.tsx:100`).
7. Command Palette: client rows → `/clients/${id}`, drop the Messages entry (`CommandPalette.tsx:38-44`).

Afternoon (the integrity fix):
8. `complianceEngine.ts` readiness/guardrails: stop returning healthy zeros on error — throw or return a tagged failure; render the existing error patterns on `/compliance-readiness` and the Dashboard cards (S3-1, S3-2).
9. `PortalDashboard.tsx`: destructure errors, route to `PortalErrorCard`, fix the "Session Expired." misdiagnosis (S3-5).
10. Define the dead tokens in `index.html`'s colors block (finding #3) and eyeball the 22 affected files.

That day removes every raw token from the two most-seen clinical surfaces, fixes the reported plural bug, makes global search work, and makes the compliance surfaces honest under failure.

## TOKEN-LEVEL — many screens at once, ports cleanly to Attesta

- **Define the 5 missing tokens** (`on-surface`, `on-surface-secondary`, `surface-secondary-content`, dark variants) — one hunk in `index.html`, 122 sites regain color. (DEFERRED #24's verification-pass caveat stands.)
- **Semantic status tokens + palette override** — add `success/warning/danger/info`; override `amber/emerald/red/rose/blue` the way `slate` already is (`index.html:38-44`). ~1,500 sites become retunable from one file. This is the Attesta lever.
- **Create the missing `/index.css`** (it's already linked): global `focus-visible` ring + the ~15-line print block. Two app-wide fixes, one new file, trivially portable.
- **Dark-mode `slate.400`**: add a dark-tuned value — fixes every `dark:text-slate-400` AA failure including shared `Card.tsx:31`.
- **`ui/Button.tsx` + `ui/Input.tsx`** (M–L, biggest single lever): 136 primary-button spellings → 1; the 58 `bg-primary`+`hover:bg-primary-focus` sites are the codemod anchor. Bundle focus-visible, disabled, and aria patterns into it once.
- **Harden `Modal.tsx`** — focus trap, Escape, `aria-labelledby`, labeled X, unmount-on-close: every modal inherits.
- **Extract `StatusChip`/`toneOf`** — DEFERRED #43 already scopes it; converges the 3–4 private copies of the red/amber/emerald tone grammar and becomes the natural home for ONE severity ladder (finding #6).
- **Shared formatters module** — `formatMoney` (Intl), `formatDate`, `daysAgo`, `plural`: replaces 6+2+0+1 private copies; every future screen inherits.
- **`<Citation>` primitive** — trivial, and it institutionalizes the app's best idea.

## DO NOT TOUCH — looks wrong, is load-bearing

- **`ClientWorkspace.tsx:309` `if (isLoading) return <LoadingSpinner/>`** + the `key={clientId}` wrapper (`:558`) — a confidentiality control, not a loading nicety (ROADMAP.md:35-50). Never remove both; a skeleton-ification pass here must preserve the remount.
- **`TRIAL_MODE` (`config/trialMode.ts`)** — hides six individually-broken surfaces; it is containment, not a release gate (ROADMAP.md:52-62). Fix the *leaks to* hidden routes (finding #2); do not flip the flag to make nav "consistent."
- **The slate override hack (`index.html:38-44`)** — reads like a smell, is the proven portability mechanism. Extend it; don't "clean it up" into per-component classes.
- **Clara's briefing line being template-generated, not AI** (`Dashboard.tsx:127-130`) — improving its content (1d) must stay deterministic. Do not "upgrade" it to a model call. 42 CFR note: keep client names out of it — it is the most prominent text on a screen that gets shoulder-surfed; aggregate counts + ages, names on click-through.
- **jsPDF same-instance preview==save** (`DocumentPreviewModal.tsx:33-36`) — any refactor that rebuilds the doc for save reintroduces drift.
- **`Modal.tsx`'s `if (!isOpen) return null` at specific call sites** — `MainLayout.tsx:214-226` deliberately conditional-renders `CreateClientModal` for PHI reasons; hardening the primitive must not resurrect state-retention at those sites.
- **The Play-CDN Tailwind setup** — custom keyframes already "silently no-op" (`index.html:109-111`); migrating to a build-time Tailwind is a real project with its own verification pass, not a drive-by cleanup to bolt onto any of the above.
- **Financials' unreachable-empty-state-on-error ordering** (`Financials.tsx:239-256`) — the `errOut` short-circuit placement is the correctness; don't "simplify" the branch order.
- **`/session/:aptId/green-room` from the Dashboard schedule** (`Dashboard.tsx:161`) — looks adjacent to hidden `/session/:clientId` but is the live GreenRoom route, correctly reachable.

---
*Recon only. No code changed; this file is the only artifact. Sweeps by four parallel read-only agents over pages/, components/, layouts/, config/, services/, contexts/, supabase/migrations/; ~200 files cited by line.*
