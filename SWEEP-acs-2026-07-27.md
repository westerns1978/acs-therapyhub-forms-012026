# HONESTY SWEEP — ACS TherapyHub — 2026-07-27

Read-only. Nothing fixed, nothing deleted, no rows touched. Second and last file created this session.
Repo `main @ a070ab9`, tree clean. Ground truth: `RECON-acs-2026-07-27.md`.
Scope: any user-visible surface asserting a capability, completion, or state the system cannot back.

**Severity key** — CLIENT-VISIBLE: a client, court, or state agency can see it. STAFF-VISIBLE: staff only.
INTERNAL: real string, currently unreachable (dead code / trial-hidden) — a loaded gun, not a live shot.

**Two corrections I made to sub-agent findings, having verified them myself:**
1. `pages/ActiveSession.tsx` was reported as unrouted (from a stale note in `ACS-TherapyHub-PROGRESS.md:69`). It **is** routed — [App.tsx:133](App.tsx) `/session/:clientId`, role-gated Director/Therapist, **not** trial-hidden. This promotes four findings (H9, H12, H13, S5) from INTERNAL to live.
2. `assignForm` was momentarily suspected of being a no-op. It is **real** ([services/api.ts:1520-1548](services/api.ts), genuine Supabase insert). That is precisely what makes H14 (the phantom) worse, not better.

---

## LEAD TABLE — severity-sorted

### CLIENT-VISIBLE (13)

| # | File:line | Claims | Actually true |
|---|---|---|---|
| H1 | [PrintPreview.tsx:99](components/PrintPreview.tsx) | Cryptographic integrity hash + named secure infrastructure | String literal, identical on every form forever; "NODE 04" refers to nothing |
| H2 | [pdfDocuments.ts:171](services/pdfDocuments.ts) | Completion Date on state form MO 650-7743 | Falls back to **today's date** when unrecorded — 17 of 19 non-demo clients |
| H3 | [complianceEngine.ts:735](services/complianceEngine.ts) | "Required forms signed" gate → printed state certificate | Only checks a status string written unconditionally on submit |
| H4 | [ClaraContext.tsx:112-117](contexts/ClaraContext.tsx) | Clara helps clients with appointments, forms, payments | Zero client data injected; only tool is `google_search` |
| H5 | [PrintPreview.tsx:85](components/PrintPreview.tsx) | "ELECTRONICALLY COMMITTED VIA THERAPYHUB AUTH" | Value is a typed text string, unbound to any identity |
| H6 | [PrintPreview.tsx:93](components/PrintPreview.tsx) | "SYSTEM TIMESTAMPED" on staff signature | `new Date()` at **render**; reprinting restamps it |
| H7 | [SuccessScreen.tsx:38](components/SuccessScreen.tsx) | "A high-fidelity copy has been attached to your patient file" | No PDF rendered, no document row written |
| H8 | [pdfDocuments.ts:219](services/pdfDocuments.ts) | "verified by the deterministic compliance engine" | For the forms gate, "verified" = someone clicked Submit |
| H9 | [SessionWrapUpModal.tsx:170](components/sessions/SessionWrapUpModal.tsx) | "This will appear in the client's portal." | `addClientAssignment` is an **empty function body** |
| H10 | [OutreachModal.tsx:284](components/OutreachModal.tsx) | "Notify probation officer" | Nothing is sent; officer name never even read |
| H11 | [PortalDocuments.tsx:236](pages/portal/PortalDocuments.tsx) | "N of 11 complete" + progress bar | Counts rows not distinct forms; can exceed 100% |
| H12 | [PortalBilling.tsx:175,181,185](pages/portal/PortalBilling.tsx) | Update Method / Download Tax Statement / Insurance Claims | Three buttons with no `onClick` at all |
| H13 | [BaseFormTemplate.tsx:225](components/BaseFormTemplate.tsx) | Form ID confirmation number | Fallback branch is `Math.random()` — matches no record |

### STAFF-VISIBLE (18)

| # | File:line | Claims | Actually true |
|---|---|---|---|
| S1 | [Compliance.tsx:129](pages/Compliance.tsx) | "Compliance Score" % | Permanently **100%** via a `NaN` guard over one mock row |
| S2 | [complianceEngine.ts:544-547](services/complianceEngine.ts) | "No active flags — every enforceable rule is met" | **Fails open** — a query error renders as all-clear |
| S3 | [Compliance.tsx:88-113](pages/Compliance.tsx) | "Bulk Export (CSV)" of sessions + balances | Real names/case numbers paired with all-zero figures |
| S4 | [SessionWrapUpModal.tsx:86-97](components/sessions/SessionWrapUpModal.tsx) | "Schedule Next Week's Session" + time slots | Slot buttons inert; books a **real** appointment at hardcoded 10:00 AM regardless |
| S5 | [SessionWrapUpModal.tsx:130](components/sessions/SessionWrapUpModal.tsx) | "Click to Apply Digital Signature" | Flips a React boolean → permanent green "Signed" badge |
| S6 | [SessionWrapUpModal.tsx:74-81](components/sessions/SessionWrapUpModal.tsx) | "Submit Charge" — $150.00, CPT 90834 | `addSessionRecord` is an **empty function body** |
| S7 | [ClientFormsTab.tsx:192](components/clients/ClientFormsTab.tsx) | "Reminder sent to client via Push & SMS." | `setTimeout(1000)` + `alert`. No sender exists in the repo |
| S8 | [CreateClientModal.tsx:256](components/clients/CreateClientModal.tsx) | "An automated welcome email … will be sent" | **No email capability exists anywhere** in the app |
| S9 | [ClientFormsTab.tsx:109](components/clients/ClientFormsTab.tsx) | "AI Verified: Signature Detected" | An unscored LLM boolean; no authentication of any kind |
| S10 | [FormDetailModal.tsx:63](components/FormDetailModal.tsx) | "Requires verified staff credentials for submission" | No credential store exists; no role check on submit |
| S11 | [formRegistry.ts:45](config/formRegistry.ts) | Assignable form "Individual Comprehensive Treatment Plan" | No component; **Fill Out** dead-ends to the library |
| S12 | [AppointmentStatusModal.tsx:527-548](components/sessions/AppointmentStatusModal.tsx) | Billable-units picker | No enabled control saves it on an already-Completed row |
| S13 | [GreenRoom.tsx:470](pages/GreenRoom.tsx) | "the **enrolled roster** from the schedule" | `group_enrollments` has **zero** readers repo-wide |
| S14 | [GreenRoom.tsx:301](pages/GreenRoom.tsx) | Present / Absent attendance toggles | React state only; never persisted anywhere |
| S15 | [CommandPalette.tsx:38,43](components/ui/CommandPalette.tsx) | ⌘K → Messages / View \<client\> | Both silently redirect to `/dashboard` |
| S16 | [ClientDocumentsGrid.tsx:106-108](components/clients/ClientDocumentsGrid.tsx) | Per-document green "SECURE" shield | Static JSX; renders even on "unverified" documents |
| S17 | [complianceEngine.ts:173](services/complianceEngine.ts) | "Plan review current" | Clock starts from an **empty, unauthored** plan row |
| S18 | [ClientOverviewTab.tsx:312](components/clients/ClientOverviewTab.tsx) | "Signed \<date\>" on treatment plan | It is `submitted_at`; no signature is read |

### INTERNAL (9 groups) — unreachable today

| # | File:line | Claims | Why contained |
|---|---|---|---|
| I1 | [ClientList.tsx:153](pages/ClientList.tsx) | "securely transmitted to the Missouri Department of Revenue" | Handler is `console.log`; **page unrouted** |
| I2 | [ClientList.tsx:212](pages/ClientList.tsx) | "Process & Issue Certificate" / "will finalize their record" | Same `console.log`; unrouted |
| I3 | [PushNotificationButton.tsx:32-35](components/ui/PushNotificationButton.tsx) | "You will now receive compliance reminders and alerts." | No `pushManager.subscribe`, no VAPID, no sender; unmounted |
| I4 | [api.ts:1623](services/api.ts) | Briefing: streak 10, 2 completions | Hardcoded; `AIBriefingModal` has no importer |
| I5 | [RiskDashboard.tsx:39](components/RiskDashboard.tsx) | "Predictive Risk Monitor" | Asserts a model; component unmounted |
| I6 | [database.ts:302-306](data/database.ts) | QuickBooks + Google Calendar `status: 'Connected'` | No QuickBooks code exists; array has no reader |
| I7 | [api.ts:1633-1640](services/api.ts) | Revenue by program; 88→98% compliance trend | Hardcoded literals; `/reporting` trial-hidden |
| I8 | [MeetingSummary.tsx:52-67](components/reports/MeetingSummary.tsx) | Four shipped feature areas, present tense | Printable deck; `/reporting` trial-hidden |
| I9 | [database.ts:35](data/database.ts) + seed SQL | "Dr. Anya Sharma" | Fabricated clinician removed from `/compliance` in `bbf805c` — **survives in mock + seed narrative** |

### AMBIGUOUS (4) — flagged, not decided

| # | File:line | Why I am not deciding it |
|---|---|---|
| A1 | [PortalDashboard.tsx:325](pages/portal/PortalDashboard.tsx) "E-sign pending forms" | A typed name *can* be a valid e-signature under ESIGN/UETA. Intent/association/retention are arguably met; **attribution is the gap**. Legal call, not a code call. |
| A2 | [WebsitePortalBridge.tsx:313](pages/WebsitePortalBridge.tsx) "Certified by the Missouri Division of Behavioral Health" | A business fact about the real clinic, almost certainly true. Verify against certification paperwork; do not change from the code side. |
| A3 | [FormLibrary.tsx:209](components/FormLibrary.tsx) "HIPAA-compliant clinical forms" | Organizational/BAA posture I cannot verify from source. Note drafts autosave to **unencrypted `localStorage`**, which a reader of that phrase would not expect. |
| A4 | [alertsService.ts:239,273](services/alertsService.ts) `tasks` / `outreach_log` | No migration creates them; cannot confirm they exist live. **Unambiguous either way:** each is referenced exactly once — the insert. There is no reader, no task list, no outreach history. Write-only. |

---

## DETAIL — the findings that matter most

### H1–H2–H3–H8 · The certificate chain (one connected defect)

This is the structural finding. Four assertions compound into one printed State of Missouri document.

**The gate.** [services/complianceEngine.ts:733-742](services/complianceEngine.ts):
```
      label: 'Required forms signed',
      detail: … `All ${requiredForms.length} required forms completed/signed.`
      citation: '9 CSR 30-3.206(13)(F)',
```
**Its only input.** [services/complianceEngine.ts:438-442](services/complianceEngine.ts):
```
    const s = String(r.status || '').toLowerCase();
    if (r.form_id && (s === 'completed' || s === 'reviewed')) out.add(r.form_id);
```
**What writes that status.** [components/BaseFormTemplate.tsx:194](components/BaseFormTemplate.tsx) — `status: 'Completed',` written unconditionally on submit. The payload contains no signature field and the write path performs no signature check. Every "signature" in the live form set is a `type: 'text'` input the user types their own name into — e.g. [HipaaAckForm.tsx:31](components/forms/HipaaAckForm.tsx):
```
    { id: 'clientSignature', label: 'Client signature (type your full legal name)', type: 'text', required: true },
```
A `Reviewed` row also counts as signed, and [services/api.ts:1494-1498](services/api.ts) explicitly permits `reviewed_by: reviewerId || null` — signed with no attributable reviewer.

**Where it terminates.** [services/pdfDocuments.ts:153-157](services/pdfDocuments.ts) prints `STATE OF MISSOURI` / `DEPARTMENT OF MENTAL HEALTH` / `SATOP COMPLETION CERTIFICATE` on form `MO 650-7743 (8-98)`, carrying [**:219**](services/pdfDocuments.ts):
> "Populated fields reflect ACS TherapyHub records verified by the deterministic compliance engine. Blank fields are not captured by the system and must be completed by the certifying Offender Management Unit."

**And the date is fabricated.** [services/pdfDocuments.ts:171](services/pdfDocuments.ts):
```
  const completionDate = client.program_end_date ?? client.programEndDate ?? new Date().toISOString();
```
This sits **four lines below** the file's own contract at [:167](services/pdfDocuments.ts) — `// Real values only — anything not genuinely in the record stays null (blank).` Every other unknown field correctly passes `null` and renders a blank rule. This one silently prints today.

**Blast radius, witnessed by query 2026-07-27:** the "DEMONSTRATION SAMPLE — NOT A VALID STATE CERTIFICATE" banner ([:158-162](services/pdfDocuments.ts)) renders only when `is_demo === true`. **19 of 34 clients have `is_demo = false`** — their certificates print clean. **17 of those 19 have a null `program_end_date`** — those print today's date as the Completion Date.

These are test clients; nothing is live-harmful today. The mechanism is armed for the first real one.

### S1–S2–S3 · `/compliance` — a live page that cannot report badly

`/compliance` is routed and role-gated to Director/Therapist ([App.tsx:155](App.tsx)) and is **not** trial-hidden. It is absent from the desktop sidebar but present in the mobile drawer ([MobileDrawer.tsx:64](components/ui/MobileDrawer.tsx)).

[pages/Compliance.tsx:68-75](pages/Compliance.tsx):
```
        const score = total > 0 ? Math.round((complete / (total - upcoming)) * 100) : 100;
        return { score: isNaN(score) ? 100 : score, overdue, upcoming };
```
The input is `getComplianceEvents()` → `dbComplianceEvents`, seeded from [data/database.ts:127-131](data/database.ts) with **exactly one** hardcoded `'upcoming'` row. So `total=1, complete=0, upcoming=1` → `0/0` → `NaN` → the guard fires → the tile renders **"100%"** in a green shield gradient ([:126-131](pages/Compliance.tsx)). There is no path on which this number reflects any client's real state. Note the team already dropped this metric once — migration `20260608_drop_compliance_score.sql` — and cleaned the portal; this staff page was missed.

Worse, the readiness page **fails open**. [services/complianceEngine.ts:540-548](services/complianceEngine.ts):
```
  if (error || !data) {
    if (error) console.warn('[complianceEngine] readiness query failed:', error.message);
    return base;
  }
```
`base` carries `flags: []`, and it is truthy — so `if (!data)` in the page never fires and `flags.length > 0` is false, rendering [ComplianceReadiness.tsx:88-91](pages/ComplianceReadiness.tsx):
> "No active flags — every enforceable rule is met."

An RLS denial, a dropped network, or a schema drift renders as an emerald all-clear on the one page whose entire job is to fail closed. The codebase demonstrably knows this pattern — [PortalDocuments.tsx:193-195](pages/portal/PortalDocuments.tsx) says *"A failed load must NEVER render as 'everything is pending' — that's a phantom state"* — the compliance path just never got the treatment.

The CSV export ([:88-113](pages/Compliance.tsx)) pulls from `getSessionRecords('')`, which filters the mock array by `clientId === ''` and matches nothing — producing a downloadable file of **real client names and case numbers paired with 0 sessions and 0.00 balance**.

### H4 · Clara tells clients she can do things she has no data for

`pds-gemini-proxy` holds **no system prompt** — I pulled its live source; it is a transparent key-injecting passthrough. Every prompt is client-side, so F3's tenant-leak concern is answered: no PDS/copier framing can leak. Its PDS name is shared-infrastructure naming only.

Clara mounts for portal clients at [layouts/PortalLayout.tsx:163](layouts/PortalLayout.tsx). [contexts/ClaraContext.tsx:112-117](contexts/ClaraContext.tsx):
```
You help clients with questions about their SATOP program, REACT program, DWI Court requirements, appointment scheduling, form completion, payment information, and general recovery support.
```
and the opening line she says unprompted, [:123](contexts/ClaraContext.tsx):
```
"Hi there! I'm Clara, your personal recovery assistant. I'm here to help you with questions about your program, appointments, forms, or anything else you need. How can I help you today?"
```

| Capability asserted | Status |
|---|---|
| Appointment scheduling / "your appointments" | **ABSENT** — no appointment data reaches the model |
| Form completion status | **ABSENT** |
| Payment information | **ABSENT** |
| Program progress | **ABSENT** |
| General recovery support | REAL (generic knowledge) |

`buildInstruction()` ([:222-225](contexts/ClaraContext.tsx)) returns the persona string with only `user.name` and `user.role` interpolated. `sendText` posts `contents: history` — the conversation transcript, nothing else ([:516-527](contexts/ClaraContext.tsx)). The file's own header concedes it at [:36-37](contexts/ClaraContext.tsx): *"NOT in Phase 1: … engine-truth injection."*

Client mode's only tool is **`google_search`** ([:233-235](contexts/ClaraContext.tsx)). So "when is my next appointment?" is answered fluently from nothing, or from the open web. **This is the fluent-failure case: no error, no broken UI, a confident wrong answer to a client in a court-mandated program.**

The staff prompt has the same shape — [:93-96](contexts/ClaraContext.tsx) promises to "Surface clinical priorities for today: pending intakes, due court reports, missed sessions, approaching compliance deadlines" and "Answer questions about client records, program progress, session schedules, and billing status." All ABSENT. Only [:110](contexts/ClaraContext.tsx) — "You can navigate the staff UI via the available tool" — is REAL (`navigate_to_page`, [:238-239](contexts/ClaraContext.tsx)).

*Positive control:* [services/cimorPacket.ts:292-300](services/cimorPacket.ts) is the honest counter-example — facts passed inline, hard constraints against inventing a level or hour count, returns `null` on failure so the packet renders without prose.

### H9, S4, S5, S6 · Session Wrap-Up — three no-ops and one phantom booking (LIVE)

Reachable at `/session/:clientId` ([App.tsx:133](App.tsx)). The wizard advances through four steps and ends on *"Session Finalized! All post-session tasks are complete. Great work!"* ([:177-178](components/sessions/SessionWrapUpModal.tsx)).

- **Charge** — [:74-81](components/sessions/SessionWrapUpModal.tsx) calls `addSessionRecord({… rate: 150.00, // Mock rate …})`. [services/api.ts:1620](services/api.ts): `export const addSessionRecord = async (record: any) => {};` — **empty body.** The real ledger is the `charges` table; this never touches it. The CPT code `90834` and `$150.00` are hardcoded display literals ([:138-141](components/sessions/SessionWrapUpModal.tsx)).
- **Homework** — [:101](components/sessions/SessionWrapUpModal.tsx) calls `addClientAssignment`. [services/api.ts:1516](services/api.ts): `export const addClientAssignment = async (assignment: any) => {};` — **empty body**, under UI text reading *"This will appear in the client's portal."*
- **Schedule Next** — the five time-slot buttons and "View Full Calendar" have **no `onClick`** ([:149-154](components/sessions/SessionWrapUpModal.tsx)). Meanwhile [:86-97](components/sessions/SessionWrapUpModal.tsx) unconditionally writes a **real** appointment hardcoded to next week `startTime: '10:00 AM'`, no conflict check, with no way to skip the step. The inverse failure: the choice is discarded and the thing nobody chose lands on the real calendar.
- **Signature** — [:130](components/sessions/SessionWrapUpModal.tsx) `onClick={() => setIsSigned(true)}` labeled *"Click to Apply Digital Signature"*, forwarded to `saveClinicalNote(..., { isSigned })` and rendered thereafter as a permanent green **Signed** badge. `clinical_notes` has no signer column and no `signed_at` — [ClinicalNoteView.tsx:12-13](components/clients/ClinicalNoteView.tsx) says so outright. Contrast the reachable importer path, [SmartNoteImporter.tsx:78](components/notes/SmartNoteImporter.tsx), which *does* gate on an explicit confirm and writes a real audit event.

### S7, S8 · Two "we contacted the client" claims with no transport

[components/clients/ClientFormsTab.tsx:187-193](components/clients/ClientFormsTab.tsx):
```
        // Simulate API call
        await new Promise(r => setTimeout(r, 1000));
        setSendingReminderId(null);
        alert('Reminder sent to client via Push & SMS.');
```
The fake latency and spinner make it read as a real network call. Nothing is sent and nothing is even logged to `client_communications`. A staff member will record "reminded client" against a form never touched — and the client's non-response becomes documented non-compliance.

[components/clients/CreateClientModal.tsx:256](components/clients/CreateClientModal.tsx):
```
An automated welcome email with portal access instructions will be sent to <strong>{formData.email}</strong>.
```
I grepped the whole tree: **no Resend, SendGrid, SMTP, nodemailer, or mail-sending edge function exists**; the only `mailto:` uses open the staff member's own client. This sits on the final confirmation step of client creation. During this week's testing it directly produces "why hasn't the client logged in?"

### S11 · The phantom — worse than a dead label

[config/formRegistry.ts:45](config/formRegistry.ts):
```
  { id: 'treatment-plan',        title: 'Individual Comprehensive Treatment Plan', category: 'Treatment', audience: 'staff', requiredForCompletion: false },
```
No component implements it; it is absent from all three renderers ([FormLibrary.tsx:33-48](components/FormLibrary.tsx), [pages/Forms.tsx:75-106](pages/Forms.tsx), [PortalFormPage.tsx:27-39](pages/portal/PortalFormPage.tsx)). But it is **not inert**: `getFormTemplates` publishes the whole registry as the assignable catalog ([services/api.ts:1511-1512](services/api.ts)), and `assignForm` — which is real — inserts a live `form_submissions` row. `ClientFormsTab` then renders it with a **Fill Out** button ([:241](components/clients/ClientFormsTab.tsx)) that navigates to `/forms?open=treatment-plan`, hits `default:` in the switch, and silently lands the user on the library with no error. The assignment is a permanent dead end sitting on the chart as an outstanding form.

**The existing DB row** (`e1000000-0000-0000-0000-0000000d1004`, client `dddd…`, `status: 'Completed'`, `data.signed: true`) is **seed data**, not evidence of a broken write path. Per instruction it has **not** been touched. It is client-visible: `PortalDocuments` selects all submissions for the client, so it renders in that client's "My Forms" as a completed treatment plan that cannot exist.

Two sibling orphans, same shape: **"Gambling Recovery Intake"** and **"Opioid Recovery Intake"** (`form_id = NULL`, both `Completed`, both seeds). The Opioid narrative still names **"Dr. Anya Sharma"** — the fabricated clinician commit `bbf805c` removed from `/compliance`. The display was fixed; the seed text and [data/database.ts:35](data/database.ts) were not.

### S12, S13, S14 · The two features you already suspected, explained

**`billable_units` — the writer is fine; the trigger is unreachable.** [services/api.ts:596](services/api.ts) and [:675-687](services/api.ts) persist correctly. But the only trigger is the "Mark Completed" button, which is `disabled={… || appointment.status === 'Completed' || !serviceType}` ([AppointmentStatusModal.tsx:548](components/sessions/AppointmentStatusModal.tsx)). The units `<select>` has **no independent save control** — no Save button, no `onBlur` write, and neither `handleSaveNotes` nor `handleReschedule` includes `billableUnits` in the patch. So on an already-Completed appointment the picker renders, prefills a suggestion, accepts an edit, and **no enabled control anywhere will persist it.** All 262 rows were completed before the column shipped (`20260715_billable_units.sql`, which explicitly forbids backfill). Two further fail-closed gates narrow it even for future completions: non-SATOP programs and any `service_type` other than `counseling` have `unitMinutes: null` ([config/billableUnits.ts:45-51](config/billableUnits.ts)) so the control never renders.

**`group_enrollments` — zero readers, and the UI says "enrolled" three times.** The migration ships the table plus a client-self-read RLS policy that is entirely unexercised. [pages/GreenRoom.tsx:470](pages/GreenRoom.tsx): *"We show the **enrolled roster** from the schedule."* Also [:429](pages/GreenRoom.tsx) `{attendees.length} enrolled` and [:491](pages/GreenRoom.tsx) *"No clients are enrolled in this session yet."* What it actually resolves ([services/greenRoom.ts:134-140](services/greenRoom.ts)) is appointment rows sharing `group_id` + `start_time` — whoever is booked into that one occurrence. A client enrolled in the group with no appointment row is invisible; a one-off drop-in reads as "enrolled."

**Attendance is React state.** [pages/GreenRoom.tsx:233](pages/GreenRoom.tsx) `useState<Set<string>>` defaulting everyone present, toggled at [:239-240](pages/GreenRoom.tsx), rendered as `{on ? 'Present' : 'Absent'}` ([:301](pages/GreenRoom.tsx)). Its only effect is narrowing note-distribution targets. Mark someone Absent and post no note and the toggle vanishes on unmount. Even when a note posts, the absent client's `appointments.status` stays `'Scheduled'`. The page's honesty panel at [:469](pages/GreenRoom.tsx) refers to *Zoom join detection*, so a reader reasonably concludes the manual toggle is the working substitute. It is not.

### Notable smaller ones

- **S15 — the trial gate leaks through ⌘K.** `NavigationSidebar` and `MobileDrawer` both filter through `isTrialHidden`; [CommandPalette.tsx:38,43](components/ui/CommandPalette.tsx) does not. "Messages" and every per-client "View \<name\>" entry silently bounce to `/dashboard` — and that per-client entry is the palette's *only* client result.
- **S17 — the plan-review clock starts on an empty plan.** [complianceEngine.ts:340-349](services/complianceEngine.ts) selects `created_at` only, never inspecting `content`. Applying a template inserts a row immediately, flipping `MO-OP-TXPLAN-REVIEW-90D` from the honest `not_enforceable` to **`met`** for 83 days — while [TreatmentPlanTab.tsx:166-170](components/clients/TreatmentPlanTab.tsx) simultaneously says *"Plan content not yet authored."* Two surfaces, same row, opposite stories.
- **H11 — the portal progress bar double-counts.** [BaseFormTemplate.tsx:172-183](components/BaseFormTemplate.tsx) deliberately excludes completed rows from its update lookup, so re-submitting **inserts a new row**; paper uploads always insert. The headline count uses raw rows while the denominator uses distinct forms. The neighbouring "pending" gauge correctly uses a deduplicated Set — only the headline and bar are wrong.
- **E19 (INTERNAL, adjacent) — drafts are keyed by form id only.** [BaseFormTemplate.tsx:68,116,229](components/BaseFormTemplate.tsx) uses `draft-${formDefinition.id}`. Part-fill a SATOP intake for client A, abandon it, open the same form for client B → **client A's answers prefill**, silently, with the button still reading "Start" (S-adjacent: the "Saved Draft %" discovery panel can never render because the writer never stores a `progress` key).
- **I9 — dead code that would be catastrophic if revived.** [pages/ClientList.tsx](pages/ClientList.tsx) is unrouted (`/clients` → `ClientWorkspace`), but contains five fully-styled action modals — DMV transmission to the Missouri DOR, "Send Compliance Alert" to a probation officer, "Update Court Status" including "Warrant Requested", and "Process & Issue Certificate" — all wired to [:140-144](pages/ClientList.tsx) `handleFormSubmit`, which is `console.log` + close.

---

## What is genuinely honest (recorded so the next pass doesn't re-litigate)

This codebase is unusually candid for its class, and the sweep should say so. `pages/Dashboard.tsx` (stat strip traces to real counts; Monthly Revenue deliberately omitted with a comment), `pages/Settings.tsx` (real PKCE OAuth; the placebo integration card and "Reset Application Data" were already deleted with explanatory comments), `pages/ComplianceReadiness.tsx` (ships an explicit "Not Yet Verifiable" card), the portal DisplayTruth surfaces (render `—` until a determination is signed), `PortalBilling` (real Stripe, explicit TEST MODE chip), `AuditTrailTable` (real `audit_logs` reads), `packetReadiness.ts`, `complianceClock.ts`, `programVocab.ts`, and the `not_enforceable` verdict design are all deliberate anti-phantom work. [config/trialMode.ts](config/trialMode.ts) is itself an honesty artifact — each hidden route carries a written reason.

The defects cluster in the surfaces built earliest and cleaned last — `PrintPreview`, `Compliance.tsx`, `ClientFormsTab`, `SessionWrapUpModal`, and the fetcher error branches — not in the engine. Several findings sit *directly beside* comments where the team caught the identical bug elsewhere and fixed it.

**One caveat on the trial-mode mitigation:** `/reporting`, `/document-intelligence`, `/video-sessions`, `/communication-center`, `/program-compliance`, and `/portal/recovery-plan` are hidden, not fixed. Findings I7, I8, and two neutralized `alert()` surfaces become live the moment `TRIAL_MODE` flips.

---

## COUNT BY CATEGORY

- **A — Registry/config entries with no implementation: 7** (treatment-plan orphan; "Completion Certificate" doc category with no producer; 5 of 9 compliance-pack program nodes unreachable = 12 orphaned rules; 3 of 4 `ComplianceEvent` types with no producer; `dbIntegrations` QuickBooks "Connected"; retired `SignedDocument` type; `qualifiedCounselorsFor` — self-documented, ambiguous)
- **B — UI copy claiming unwired capability: 10** (3 PrintPreview attestations; welcome email; Push & SMS reminder; "high-fidelity copy"; verified staff credentials; push notifications; MeetingSummary feature deck; "HIPAA-compliant" — ambiguous)
- **C — Status labels settable without the artifact: 16** (forms-signed gate; fabricated completion date; 3 print attestations; "AI Verified"; "Apply Digital Signature"; Reviewed-with-null-reviewer; portal count; plan-review clock; plan "Signed"; unconditional SECURE badge; compliance score; staff credentials; E-sign — ambiguous; DBH certified — ambiguous; dead-code claims)
- **D — Numbers from a mock, constant, or stub: 7** (compliance score permanently 100%; all-zero CSV over real names; CSR timeline from one hardcoded event; hardcoded revenue + compliance trend; always-zero Achievements badge; briefing streak/completions; `Math.random()` form ID)
- **E — Shipped features quietly doing nothing: 15** (`addClientAssignment` no-op; `addSessionRecord` no-op; inert time slots + phantom booking; Push/SMS reminder; 3 dead portal billing buttons; probation notify; `billable_units` unreachable; `group_enrollments` zero readers; Green Room attendance; `capacity` never read; notification bell; ⌘K dead routes; Saved-Draft panel; `tasks`/`outreach_log` write-only; 7 unrouted pages + 18 unreferenced components)
- **F — AI prompts asserting absent capabilities: 9** (client mode: 4 absent capabilities + greeting; staff mode: 4 absent capabilities; 1 real — `navigate_to_page`. `pds-gemini-proxy` holds no prompt; no tenant leak; `cimorPacket` prompt is clean)

**Totals: 40 live findings (13 CLIENT-VISIBLE, 18 STAFF-VISIBLE, 9 INTERNAL groups), 4 flagged AMBIGUOUS.**

— End of sweep. Read-only; nothing fixed, nothing deleted, the phantom row left in place for David. Awaiting GO for the 1b fix pass.
